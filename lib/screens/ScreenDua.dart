import 'package:flutter/material.dart';

class ScreenDua extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("History Appointment"),
    );
  }
}
